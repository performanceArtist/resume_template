mixin avatar(options)
  -let{name, surname, occupation} = options
  .avatar
    .avatar__img
      img(src=options.url)
    .avatar__name #{name}
    .avatar__surname #{surname}
    .avatar__occupation #{occupation}