include ../../components/section-title/section-title
include ../../components/info/info

mixin experience(experiences = [{}])
  section.experience
    .experience__header
      +section-title({icon:'fas fa-suitcase', title:'EXPERIENCE'})
    .experience__content
      ul.experience__list
        each experience in experiences
          .experience__info
            +info(experience)